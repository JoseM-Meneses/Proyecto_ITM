server:
  port: 8010

spring:
  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=tenis_col;encrypt=false
    username: sa
    password: 89985122Jm.
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  jpa:
    database-platform: org.hibernate.dialect.SQLServerDialect

    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true